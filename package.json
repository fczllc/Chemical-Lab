{
  "name": "element-explorer-kids",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:elements": "node scripts/validate-elements.mjs",
    "validate:supporting": "node scripts/validate-supporting-data.mjs",
    "validate:spectral": "node scripts/validate-spectral-lines.mjs",
    "validate:data": "npm run validate:elements && npm run validate:supporting && npm run validate:spectral && node scripts/audit-business-data-imports.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "gsap": "^3.12.7",
    "three": "^0.172.0"
  }
}
