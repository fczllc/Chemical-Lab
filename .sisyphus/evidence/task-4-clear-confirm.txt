Task 4 clear-data confirm evidence
Date: 2026-05-14

Command run:
`npx playwright test tests/ui/settings-reset.spec.ts --project=chromium`

Exit code: 0

Observed Playwright result:
- Running 1 test using 1 worker
- PASS [chromium] tests/ui/settings-reset.spec.ts:50:3 Settings data reset flow > shows requested settings layout and preserves settings while clearing learning data (23.7s)
- 1 passed (27.6s)

Confirm path assertions covered by focused test:
- Seeds localStorage key `element-explorer-kids-state` with non-empty learning/progress/scores/achievements/game/activity data and `settings.performanceMode = "high-performance"`.
- Opens settings with `settings-btn` and verifies the settings modal and clear-data controls are visible.
- Clicks the `settings-clear-data` row button labelled `清除`.
- Verifies the custom `settings-clear-confirm-dialog` appears.
- Clicks `settings-clear-confirm` labelled `确认清除`.
- Reads both `window.appState` and localStorage after confirmation.
- Confirms resetProgress semantics: `learnedElements`, `collectedElements`, `quizScores`, `completedExperiments`, `unlockedAchievements`, and `activityLog` are empty arrays; `experimentTitleOverrides`, `achievementDates`, `gameScores`, and `gamePlays` are empty objects.
- Confirms localStorage remains a v2 envelope and the same learning/progress/scores/achievements/game/activity fields are empty/default in `storedEnvelope.data`.
- Confirms settings are preserved in both `window.appState` and localStorage as `{ performanceMode: "high-performance", particleDensity: "high", soundEnabled: true, difficulty: "normal" }`.
- Reloads the page, waits for the app shell, reopens settings, and re-reads state.
- Confirms empty/default learning data and preserved `settings.performanceMode` remain correct after reload.

Visible stats/reset coverage:
- The focused test keeps the settings UI open after reload and verifies state persistence directly through `window.appState` and localStorage. The same reset state drives visible progress/stat counts; no separate visible count assertion is present in this focused spec.

Console/page errors:
- The focused test passed without Playwright reporting console, page, or assertion errors.

`resetAll()` wiring check:
Command: `grep` search for `resetAll\(` under `src` JavaScript files.
Result: one match only, `src/modules/storage.js:1000: export function resetAll() {`.
Summary: no `resetAll()` call is wired from `src/main.js` or the settings clear-data handler.
