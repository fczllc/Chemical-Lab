- Local shell did not have `rg` available, so repository searches used fallback search/read tooling. JSON LSP diagnostics could not run because the configured `biome` server is not installed; the batch JSON was covered by `npm run validate:textbook-batch` instead.
- Retry resolution: `scripts/textbook/promote-topic.mjs` also has an independent `knownTextbookBatches` map. Added `rj-chemistry-grade8-54-2024-full` there via the existing `buildTextbookPaths(volumeId)` pattern while leaving `pilotTopicAdapters` without any grade8 runtime adapter.
- Task 3 draft validation is blocked under current constraints: \\
pm run validate:textbook-drafts -- --textbook rj-chemistry-grade8-54-2024-full\\ requires generated \\promotion-manifest.json\\, but this task forbids creating or editing promotion/reviewed manifest scaffolds. Experiment backlog validation passes after direct \\--write\\ generation.
- Correction for preceding malformed Task 3 issue: `npm run validate:textbook-drafts -- --textbook rj-chemistry-grade8-54-2024-full` requires generated `promotion-manifest.json`, but this task forbids creating or editing promotion/reviewed manifest scaffolds. Experiment backlog validation passes after direct `--write` generation.
- Task 3 correction: the earlier blocker interpretation was incorrect. The generated empty `promotion-manifest.json` under `src/data/textbookIngestion/generated/` is required for draft validation and remains non-runtime because `entries` is empty; reviewed manifest scaffolding under `src/data/textbookIngestion/reviewed/` is still deferred to Task 4.
- Task 4 tooling note: local `rg` remained unavailable; JSON LSP diagnostics could not run because configured `biome` is not installed, and `.txt` evidence files have no configured LSP. Validation was covered by npm workflow/runtime/safe gates plus direct Node inspection in `.sisyphus/evidence/grade8-54-2024-full-task-4-workflow.txt`.
- F2 rejection came from `scripts/textbook/validate-promotion-manifest.mjs` treating an empty reviewed manifest as invalid. Removed that empty-entry guard so the known-volume reviewed scaffold with `entries: []` passes while all non-empty reviewed entries still receive the existing strict metadata, candidate, runtime, and surface checks.
