## 2026-04-28T21:43:25.9710908+08:00 Task: context-bootstrap
- Safe default styling surface is `index.html` plus `src/styles/base.css`, `layout.css`, `periodic-table.css`, `panel.css`, `games.css`, `lab.css`, `achievements.css`, and `responsive.css`.
- `Orbitron` + `Noto Sans SC` already fit the target direction; prefer rebalancing hierarchy, spacing, and glow intensity over replacing typography.
- The shell should read as a static dark console, not an animated sci-fi scene.
- For dark HUD CSS, prefer tokenized glow tiers, `:focus-visible` on dark backgrounds, and `prefers-reduced-motion` coverage.
